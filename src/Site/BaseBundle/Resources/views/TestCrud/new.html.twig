{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TestCrud creation</h1>

    <form action="{{ path('testcrud_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('testcrud') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
